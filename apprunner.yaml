version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: streamlit run app.py --server.port 8501 --server.address 0.0.0.0
  network:
    port: 8501
    env: APP_PORT
  env:
    - name: APP_PORT
      value: "8501"